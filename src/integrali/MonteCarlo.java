package integrali;

public class MonteCarlo {
	public static void main(String...strings ){
		try {
			int n=Integer.parseInt(strings[0]);
			int a=Integer.parseInt(strings[1]);
			int b=Integer.parseInt(strings[2]);
		}catch(Exception e) {
			System.out.println("Three arguments needed (value, a, b).");
		}
		int interni=0;
		int totali=0;
		//int b=Math.ceil(Math.log(50)
		for(long i=0;i<Long.MAX_VALUE/4; i++ ) {
			
		}
	}

}
